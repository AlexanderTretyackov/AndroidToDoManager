<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools">
    <CheckBox
        android:id="@+id/checkBoxCompleted"
        android:layout_width="24dp"
        android:layout_height="24dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/priorityImageView"
        android:layout_width="16dp"
        android:visibility="gone"
        android:layout_height="20dp"
        android:layout_marginStart="12dp"
        app:layout_constraintBottom_toBottomOf="@+id/checkBoxCompleted"
        app:layout_constraintStart_toEndOf="@+id/checkBoxCompleted"
        app:layout_constraintTop_toTopOf="@+id/checkBoxCompleted"
        tools:src="@drawable/high" />

    <TextView
        android:id="@+id/toDoNameTextView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textColor="@color/black"
        android:textSize="16sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toEndOf="@+id/barrierCheckboxAndImage"
        app:layout_constraintTop_toTopOf="@+id/checkBoxCompleted"
        tools:text="Купить что-то, где-то, зачем-то, но зачем не очень понятно" />

    <androidx.constraintlayout.widget.Barrier
        android:id="@+id/barrierImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:barrierDirection="right"
        app:barrierMargin="3dp"
        app:constraint_referenced_ids="priorityImageView"/>
    <androidx.constraintlayout.widget.Barrier
        android:id="@+id/barrierCheckbox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:barrierDirection="right"
        app:barrierMargin="12dp"
        app:constraint_referenced_ids="checkBoxCompleted"/>
    <androidx.constraintlayout.widget.Barrier
        android:id="@+id/barrierCheckboxAndImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:barrierDirection="right"
        app:constraint_referenced_ids="barrierCheckbox,barrierImage"/>

    <TextView
        android:id="@+id/itemDeadlineTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="15 февраля 2024"
        android:textSize="16sp"
        android:textColor="#B3B3B3"
        app:layout_constraintStart_toStartOf="@+id/toDoNameTextView"
        app:layout_constraintTop_toBottomOf="@+id/toDoNameTextView" />
</androidx.constraintlayout.widget.ConstraintLayout>